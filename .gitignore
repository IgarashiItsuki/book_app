# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# Temporary files generated by your text editor or operating system
# belong in git's global ignore instead:
# `$XDG_CONFIG_HOME/git/ignore` or `~/.config/git/ignore`

# ---------------------------------------------------
# 一般的な Rails アプリで無視すべきファイル & フォルダ
# ---------------------------------------------------

# Bundler 設定ファイル (ローカルに作成される Gem 管理情報)
.bundle/

# 環境変数ファイル (.env ファイルが存在する場合)
.env
.env.*
.envrc

# ログファイル (エラーログやサーバーログなど)
log/*
!log/.keep  # フォルダを保持するためのファイルは除外しない

# 一時ファイル (キャッシュやビルドファイルなど)
tmp/*
!tmp/.keep  # フォルダを保持するためのファイルは除外しない

# Active Storage (Rails のファイルアップロード機能)
storage/*
!storage/.keep  # フォルダを保持するためのファイルは除外しない

# テンポラリストレージ (特に開発時のファイルアップロード)
tmp/storage/*
!tmp/storage/.keep

# プリコンパイルされたアセット (Rails が生成する静的ファイル)
public/assets
public/packs
node_modules/

# Rails のシークレットキー (機密情報を含むファイル)
config/master.key
config/credentials.yml.enc

# SQLite3 データベース (ローカル開発用のデータベース)
db/*.sqlite3
db/*.sqlite3-journal

# 環境ごとの設定ファイル (特にデプロイ時に使われることが多い)
config/settings.local.yml
config/settings/*.local.yml
config/environments/*.local.rb

# ---------------------------------------------------
# 開発環境やエディタ関連のファイル
# ---------------------------------------------------

# Visual Studio Code の設定
.vscode/
.ruby-lsp/

# IDE が生成する設定ファイル
.idea/             # RubyMine や IntelliJ IDEA 用
*.iml              # IntelliJ IDEA のプロジェクトファイル

# Docker 関連の設定 (もしプロジェクトで使用している場合)
.dockerignore
Dockerfile
docker-compose.yml

# 編集中の一時ファイル (バックアップファイルなど)
*.swp
*.bak
*~

# Gem のキャッシュディレクトリ (Bundler)
vendor/bundle/

# ---------------------------------------------------
# その他：Rails アプリ固有の設定
# ---------------------------------------------------

# Kamal などのデプロイツールが生成する設定ファイル (存在する場合)
.kamal/

# 一部のシステムで自動生成される不要なファイル
Thumbs.db
.DS_Store
